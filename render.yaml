services:
  - type: web
    name: news-backend
    env: node
    plan: free
    branch: develop
    rootDir: backend # вказуємо директорію з файлом server.js
    buildCommand: ""
    startCommand: "npm start" # запускаємо server.js
    autoDeploy: true
